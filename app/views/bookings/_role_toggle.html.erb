<div class="role-toggle">
  <span class="role-toggle-label">Pro</span>
  <%= link_to bookings_path(view: view == "artist" ? "client" : "artist"),
        class: "role-toggle-switch #{'role-toggle-switch--on' if view == "artist"}" do %>
    <span class="role-toggle-knob"></span>
  <% end %>
</div>
