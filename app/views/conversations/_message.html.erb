<div id="<%= dom_id(message) %>" class="message-group" data-sender-id="<%= message.user_id %>">
  <div class="message-bubble">
    <%= message.body %>
  </div>
  <span class="message-timestamp"><%= message.created_at.strftime("%H:%M") %></span>
</div>
